https://meet.google.com/ebp-vtpc-xxa

- Meu trabalho seria baseado na primeira ideia apresentada na especificação do T4.
- Seria uma linguagem de alto nível (?) que gera código na linguagem de programação do Arduino.

INICIO FUNCAO void setup;

	CHAMAR FUNCAO Serial.begin(SPEED); // Gera a linha: "Serial.begin(SPEED);".
	// Chama a função do Arduino Serial.begin que configura a taxa de transferência em bits por segundo (baud rate) para transmissão serial.  //gera a linha: "Serial.begin(SPEED);"

FIM FUNCAO void setup;

INICIO FUNCAO void loop
	VARIAVEL TIPO int CHAMADA sensorValue; // Gera a linha: "int sensorValue;".
	//Declara uma variável. 
	
	sensorValue = CHAMAR FUNCAO analogRead(PINO); //Gera a linha "sensorValue = analogRead(PINO);"
	//Atribui a variavel sensorValue o valor retornado pela função analogRead do Arduino com parâmetro PINO. 

	CHAMAR FUNCAO Serial.println(val); //Gera a linha "Serial.println(val);". 
	//Serial.println imprime dados na porta serial como texto ASCII seguido pelo caratere de retorno de carruagem (ASCII 13, ou '\r') e um cactere de nova linha (ASCII 10, ou '\n').
	
	CHAMAR FUNCAO delay(ms); //Gera a linha "delay(ms);"
	// Pausa o programa por uma quantidade especificada de tempo (em milissegundos). Cada segundo equivale a 1000 milissegundos.

FIM FUNCAO loop